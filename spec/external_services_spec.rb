require 'spec_helper'

describe ExternalServices do
  it 'has a version number' do
    expect(ExternalServices::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
